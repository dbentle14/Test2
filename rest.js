// Rest and Spread operator (...)

// Rest - Combine multiple elements into an array
// Spread - spread an aray into multiple elements (unpacking all of the elements)
// They are exact opposites of each other

function add (a, b){
    console.log(a+b);
}

add (1,2);
add(1,2,3);
add(1,2,3,4);